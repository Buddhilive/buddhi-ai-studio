[project]
name = "buddhi-ai-studio"
version = "0.1.0"
readme = "README.md"
description = "Buddhi AI Studio"

requires-python = ">=3.9"

dependencies = [
    "pyloid",
    "pyloid-adapter",
    "fastapi",
    "uvicorn",
    "llama-cpp-python>=0.3.16",
]

[dependency-groups]
dev = [
    "pyloid-builder",
    "pyloid-watcher",
    "ruff",
]
